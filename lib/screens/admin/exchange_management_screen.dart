import 'package:flutter/material.dart';

class ExchangeManagementScreen extends StatelessWidget {
  const ExchangeManagementScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ExchangeManagementScreen'));
  }
}
